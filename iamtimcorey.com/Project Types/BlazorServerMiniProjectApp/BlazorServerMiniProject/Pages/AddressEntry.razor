@page "/address"

<EditForm Model="model" OnValidSubmit="ValidSubmit">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <InputText @bind-Value="model.StreetAddress" placeholder="Street" class="form-control"/>
    <InputText @bind-Value="model.City" placeholder="City" class="form-control" />
    <InputText @bind-Value="model.State" placeholder="State" class="form-control" />
    <InputText @bind-Value="model.ZipCode" placeholder="Zip Code" class="form-control" />

    <button class="btn btn-primary" type="submit">Submit</button>
</EditForm>

<h3>Addresses List</h3>
<ul>
    @foreach (var a in addresses)
    {
        <li>@a.StreetAddress, @a.City, @a.State  @a.ZipCode</li>
    }
</ul>

@code {
    private AddressModel model = new();
    private List<AddressModel> addresses = new();

    private void ValidSubmit()
    {
        addresses.Add(model);
        model = new();
    }
}
